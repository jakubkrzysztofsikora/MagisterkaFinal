<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:graphx="http://schemas.panthernet.ru/graphx/"
        xmlns:VisualEcosystem="clr-namespace:Magisterka.VisualEcosystem" x:Class="Magisterka.MainWindow"
        mc:Ignorable="d"
        Title="MainWindow" ResizeMode="NoResize" WindowState="Maximized">
    <Window.Resources>
        <ContextMenu x:Key="NodeContextMenu">
            <MenuItem Header="Set as starting point" />
            <MenuItem Header="Set as target" />
        </ContextMenu>
    </Window.Resources>
    <Grid>
        <graphx:ZoomControl x:Name="ZoomControl">
            <VisualEcosystem:VisualMap x:Name="VisualMap"/>
        </graphx:ZoomControl>
        <Menu x:Name="menu" HorizontalAlignment="Left" Height="17" VerticalAlignment="Top" Width="519">
            <MenuItem Header="_Releout Graph" Height="17" Click="MenuItem_Click" />
        </Menu>
    </Grid>
</Window>
